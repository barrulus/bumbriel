// warm / night tint — full-screen colour grade (no cursor)
global-shader {
    enable
    source "vec4 global_color(vec3 c){ vec3 s=tex2D_screen(c.xy).rgb; return vec4(s*vec3(1.05,0.92,0.78), 1.0); }"
}
