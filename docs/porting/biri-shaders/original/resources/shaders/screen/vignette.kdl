// vignette — darken toward the screen edges (no cursor)
global-shader {
    enable
    source "vec4 global_color(vec3 c){ vec3 s=tex2D_screen(c.xy).rgb; float v=smoothstep(0.85,0.35,length(c.xy-0.5)); return vec4(s*mix(0.5,1.0,v), 1.0); }"
}
